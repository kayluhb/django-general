============
Installation
============

At the command line::

    $ easy_install django-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-utils
    $ pip install django-utils
