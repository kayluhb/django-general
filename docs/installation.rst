============
Installation
============

At the command line::

    $ easy_install django-general

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-general
    $ pip install django-general
